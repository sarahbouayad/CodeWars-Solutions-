function cockroachSpeed(s) {
    //Good Luck!
    console.log(s)
  // write a function that takes its speed in km per hour, returns in cm per second, 
  // rounded to the integer (= floored)
  //   P: speed in km per hour
  //   R: return cm per second, rounded down to the integer 
  //   E: 1.08 ---> 30 
  
  return Math.floor(s * 27.777778)
  }

  