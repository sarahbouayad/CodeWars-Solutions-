




const swap = string => 
// use replace as function
// / [] / regular expression, g is global
// toUpperCase to capitalize letters
// element indiciating elements of string. 
string.replace(/[a,e,i,o,u]/g, element => element.toUpperCase())